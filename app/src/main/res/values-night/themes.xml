<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.GlamEssence" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">#FFFFFF</item>       <!-- White background -->
        <item name="android:navigationBarColor">#FFFFFF</item>   <!-- White bottom bar -->
        <item name="android:windowLightStatusBar">true</item>
    </style>
    <style name="Theme.GlamEssence.Launch" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>


</resources>
